import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import App from '../../../../.emacs.d/tmp/backups/!home!thartman!projects!ble-remote!webapp!src!App.tsx~'
import './index.css'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
